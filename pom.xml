<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.benchmarx</groupId>
  <artifactId>benchmarxUpdates-parent</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>BenchmarX Updates - Root Parent</name>

  <!--
    Vendored Maven repository for JARs that aren't on Maven Central (BXtend/BXAgent tool
    implementations, MediniQVT/qvtemf, emf.compare). The actual jar bytes live
    once each under the relevant example's lib/ folder (kept there so Eclipse .classpath
    entries, which reference lib/ directly, keep working); repo/ holds only symlinks back to
    those files plus small generated poms, so a fresh clone resolves everything with no
    manual setup step. See vendor-deps.sh to add a new non-Central jar.
  -->
  <repositories>
    <repository>
      <id>vendored</id>
      <url>file://${maven.multiModuleProjectDirectory}/repo</url>
    </repository>
  </repositories>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>17</maven.compiler.release>
    <!-- EMF versions (all on Maven Central) -->
    <emf.version>2.42.0</emf.version>
    <emf.xmi.version>2.40.0</emf.xmi.version>
    <xtend.version>2.40.0</xtend.version>
    <!-- JUnit 5 -->
    <junit.version>5.14.1</junit.version>
  </properties>

  <!-- Shared dependency management used by all child modules -->
  <dependencyManagement>
    <dependencies>
      <!-- EMF core (Maven Central: org.eclipse.emf) -->
      <dependency>
        <groupId>org.eclipse.emf</groupId>
        <artifactId>org.eclipse.emf.ecore</artifactId>
        <version>${emf.version}</version>
      </dependency>
      <dependency>
        <groupId>org.eclipse.emf</groupId>
        <artifactId>org.eclipse.emf.ecore.xmi</artifactId>
        <version>${emf.xmi.version}</version>
      </dependency>
      <dependency>
        <groupId>org.eclipse.emf</groupId>
        <artifactId>org.eclipse.emf.common</artifactId>
        <version>${emf.version}</version>
      </dependency>
      <!-- emf.compare is NOT on Maven Central; resolved from the vendored repo/ repository -->
      <dependency>
        <groupId>org.eclipse.emf.compare</groupId>
        <artifactId>org.eclipse.emf.compare</artifactId>
        <version>3.5.3</version>
      </dependency>
      <!-- Xtend runtime (needed by xtend-gen sources) -->
      <dependency>
        <groupId>org.eclipse.xtend</groupId>
        <artifactId>org.eclipse.xtend.lib</artifactId>
        <version>${xtend.version}</version>
      </dependency>
      <dependency>
        <groupId>org.eclipse.xtext</groupId>
        <artifactId>org.eclipse.xtext.xbase.lib</artifactId>
        <version>${xtend.version}</version>
      </dependency>
      <!-- Guava (pulled in transitively by Xtend, declared explicitly for clarity) -->
      <dependency>
        <groupId>com.google.guava</groupId>
        <artifactId>guava</artifactId>
        <version>33.4.8-jre</version>
      </dependency>
      <!-- Logging -->
      <dependency>
        <groupId>log4j</groupId>
        <artifactId>log4j</artifactId>
        <version>1.2.17</version>
      </dependency>
      <!-- JUnit 5 -->
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>${junit.version}</version>
      </dependency>
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter-params</artifactId>
        <version>${junit.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.2</version>
          <configuration>
            <!-- JUnit 5 provider is auto-detected when junit-platform-launcher is on classpath -->
            <includes>
              <include>**/*Test.java</include>
              <include>**/Batch*.java</include>
              <include>**/Incremental*.java</include>
              <include>**/Roundtrip*.java</include>
              <include>**/Conflicts.java</include>
              <include>**/MonotonicCreating.java</include>
              <include>**/MonotonicDeleting.java</include>
              <include>**/NonMonotonic.java</include>
            </includes>
          </configuration>
        </plugin>
        <!-- Compiles .xtend sources to xtend-gen/*.java during the build, so the
             (gitignored) xtend-gen directory no longer needs to be pre-populated
             by an Eclipse checkout. Child modules just declare the bare plugin. -->
        <plugin>
          <groupId>org.eclipse.xtext</groupId>
          <artifactId>xtend-maven-plugin</artifactId>
          <version>${xtend.version}</version>
          <executions>
            <execution>
              <goals>
                <goal>compile</goal>
              </goals>
            </execution>
          </executions>
          <configuration>
            <outputDirectory>${basedir}/xtend-gen</outputDirectory>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <!-- HTML test report: mvn surefire-report:report -->
  <reporting>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-report-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
    </plugins>
  </reporting>

  <modules>
    <module>core/Benchmarx</module>
    <module>examples/pdb1topdb2</module>
    <module>examples/asttodag</module>
    <module>examples/bag1tobag2</module>
    <module>examples/settooset</module>
    <module>examples/gantttocpm</module>
    <module>examples/pntopnw</module>
    <module>examples/familiestopersons</module>
    <module>examples/ecoretosql</module>
  </modules>

</project>
