<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.benchmarx</groupId>
    <artifactId>gantttocpm-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>BenchmarxGanttToCPM</artifactId>
  <packaging>jar</packaging>
  <name>BenchmarX - Gantt to CPM Tests</name>

  <build>
    <sourceDirectory>src</sourceDirectory>
    <testResources>
      <testResource>
        <directory>resources</directory>
      </testResource>
      <testResource>
        <directory>.</directory>
        <includes>
          <include>*.xmi</include>
          <include>*.corr.xmi</include>
        </includes>
      </testResource>
    </testResources>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <testClassesDirectory>${project.build.outputDirectory}</testClassesDirectory>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <!-- Reactor siblings -->
    <dependency>
      <groupId>org.benchmarx</groupId>
      <artifactId>Benchmarx</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.benchmarx</groupId>
      <artifactId>Gantt</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.benchmarx</groupId>
      <artifactId>CPM</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- EMF -->
    <dependency>
      <groupId>org.eclipse.emf</groupId>
      <artifactId>org.eclipse.emf.ecore.xmi</artifactId>
    </dependency>
    <!-- emf.compare: installed locally via install-local-deps.sh -->
    <dependency>
      <groupId>org.eclipse.emf.compare</groupId>
      <artifactId>org.eclipse.emf.compare</artifactId>
    </dependency>

    <!-- Logging -->
    <dependency>
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
    </dependency>

    <!-- JUnit 5 -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-params</artifactId>
    </dependency>

    <!--
      Tool JARs – installed into the local Maven repo via install-local-deps.sh.
    -->
    <!-- BXtend Gantt2CPM: lib/bxtend-gantt2cpm-1.0.0.jar (pre-built) -->
    <dependency>
      <groupId>org.benchmarx.tools</groupId>
      <artifactId>bxtend-gantt2cpm</artifactId>
      <version>1.1.0</version>
    </dependency>
    <!--
      de-tbuchmann-bxagent-gantt2cpm: built from ~/work/git/forschung/bxagent-transformations/de.tbuchmann.bxagent.gantt2cpm
      Contains de.tbuchmann.bxagent.gantt2cpm.Gantt2CpmTransformation used by BXAgentGantt2Cpm.
      Supersedes the old org.benchmarx.tools:bxagent-gantt2cpm.
    -->
    <dependency>
      <groupId>de.tbuchmann.bxagent</groupId>
      <artifactId>de-tbuchmann-bxagent-gantt2cpm</artifactId>
      <version>1.0.0-SNAPSHOT</version>
    </dependency>
    <!-- bxlang-gantt2cpm: built from Gantt2CPM-bxlang Eclipse project (see CAUTION §2) -->
    <dependency>
      <groupId>org.benchmarx.tools</groupId>
      <artifactId>bxlang-gantt2cpm</artifactId>
      <version>1.0.0</version>
    </dependency>
    <!--
      bx-runtime: dev.bxagent.correspondence.* (CorrespondenceModel, TransformationContext, etc.)
      used by BXAgentGantt2Cpm. Supersedes the old org.benchmarx.tools:emt-agent.
    -->
    <dependency>
      <groupId>dev.bxagent</groupId>
      <artifactId>bx-runtime</artifactId>
      <version>1.0.0-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>org.benchmarx.tools</groupId>
      <artifactId>mediniQVT</artifactId>
      <version>1.0.0</version>
    </dependency>
    <dependency>
      <groupId>org.benchmarx.tools</groupId>
      <artifactId>qvtemf</artifactId>
      <version>1.0.0</version>
    </dependency>
    <!-- commons-io: needed by MediniQVT implementation (NullOutputStream) -->
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <version>2.18.0</version>
      <optional>true</optional>
    </dependency>
  </dependencies>

</project>
